#include "s21_sprintf.h"
#include "s21_string.h"

#include <stdlib.h>
#include <string.h>
#include <stdio.h>

int main(void) {
/*     char spec[] = "%-+10.5lo";
    unsigned long int a = 0;
    a = -1;


    char stra[255] = {'\0'};

    a = s21_sprintf(stra, spec, 0);
    printf("s21: |%d|%s|\n", stra);


    char strb[255] = {'\0'};
    long int b = -50;

        sprintf(strb, spec, a);
    printf("std: |%s|", strb);

        sprintf(strb, spec, 0);
    printf("std: |%d|%s|\n", strb); */

